(package-initialize)

(add-to-list 'load-path "~/.emacs.d/lisp/")

;; 插件管理
;; -------------------------------------------------
(require 'init-packages)

;; 键位绑定
;; -------------------------------------------------
(require 'init-key)

;; ui设置
;; -------------------------------------------------
(require 'init-ui)

;; 修改默认配置
;; -------------------------------------------------
(require 'init-default)
